# Example environment variables — DO NOT commit real secrets here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Add other keys you need locally, but keep real values out of the repo.

# Admin credentials (example only) — DO NOT commit real passwords.
ADMIN_EMAIL=
ADMIN_PASSWORD=